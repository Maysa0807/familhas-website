services:
  oracle:
    image: gvenzl/oracle-free:23-slim
    container_name: familhas-oracle
    environment:
      ORACLE_PASSWORD: ${ORACLE_PASSWORD:-oracle}
      ORACLE_DATABASE: ${ORACLE_DATABASE:-FAMILHASPDB}
      APP_USER: ${ORACLE_APP_USER:-familhas}
      APP_USER_PASSWORD: ${ORACLE_APP_USER_PASSWORD:-familhas123}
    ports:
      - "${ORACLE_PORT:-1521}:1521"
    healthcheck:
      test: ["CMD-SHELL", "healthcheck.sh"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 60s
    volumes:
      - oracle_data:/opt/oracle/oradata
  rabbitmq:
    image: rabbitmq:3.13-management
    container_name: familhas-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-guest}
    ports:
      - "${RABBITMQ_PORT:-5672}:5672"
      - "${RABBITMQ_MANAGEMENT_PORT:-15672}:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_running"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 20s
  keycloak:
    image: quay.io/keycloak/keycloak:26.1
    container_name: familhas-keycloak
    command: ["start-dev", "--http-port=8080"]
    environment:
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN_USER:-admin}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
      KC_HOSTNAME_STRICT: "false"
    ports:
      - "${KEYCLOAK_PORT:-8081}:8080"
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1/8080"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 60s
  mailhog:
    image: mailhog/mailhog:v1.0.1
    container_name: familhas-mailhog
    ports:
      - "${MAILHOG_SMTP_PORT:-1025}:1025"
      - "${MAILHOG_UI_PORT:-8025}:8025"

volumes:
  oracle_data:
